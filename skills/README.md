# Skills (Scaffold)

This folder is intentionally a scaffold for the workshop.

Suggested skills to build:
- route_planner: graph search + feasibility checks
- pricing_agent: pricing modifiers + currency rules
- risk_reviewer: flags risky itineraries + recommends tests
- verifier: schema + invariant checker (hard fail on violations)

Keep Skill inputs/outputs as strict JSON schemas.
